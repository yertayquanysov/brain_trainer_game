import 'package:flutter_test/flutter_test.dart';

void main() {
  test("test generate position count", () async {
    /* expect(3, List.generateItemPositions(positionCount: 3, max: 1000).length);
    expect(30, List.generateItemPositions(positionCount: 30, max: 444).length);
    expect(10, List.generateItemPositions(positionCount: 10, max: 555).length);
    expect(
        300, List.generateItemPositions(positionCount: 300, max: 555).length);*/
  });
}
