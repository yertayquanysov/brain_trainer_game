name: gamx
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.15.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.1
  flutter_bloc: ^8.0.1
  logger: ^1.1.0
  google_fonts: ^2.3.1
  superellipse_shape: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true