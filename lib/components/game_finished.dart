import 'package:flutter/material.dart';

class GameFinished extends StatelessWidget {
  const GameFinished({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("The end!");
  }
}
